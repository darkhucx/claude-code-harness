# Plans.md - Claude Code Harness 開発計画

> **運用モード**: Solo
> **現在バージョン**: 2.5.23

---

## マーカー凡例

| マーカー | 意味 | 備考 |
|---------|------|------|
| `cc:TODO` | 未着手 | Impl（Claude Code）が実行予定 |
| `cc:WIP` | 作業中 | Impl が実装中 |
| `cc:完了` | 実装完了 | レビュー待ち |
| `cc:blocked` | ブロック中 | 依存タスク待ち |
| `pm:依頼中` | PM から依頼 | 2-Agent 運用時 |
| `pm:確認済` | PM 確認済 | 2-Agent 運用時 |

> **互換**: `cursor:依頼中` / `cursor:確認済` は `pm:*` と同義として扱います。

---

## フェーズ6: テンプレート追跡機能 `cc:完了`

> **目的**: プラグイン更新後、生成済みファイル（CLAUDE.md等）を最新テンプレートに同期できるようにする

### 基盤実装

- [x] `templates/template-registry.json` の作成 `cc:完了`
  - 各テンプレートの出力先パスとバージョンを定義
  - CI チェック用のソース・オブ・トゥルース

- [x] `scripts/template-tracker.sh` の作成 `cc:完了`
  - generated-files.json の初期化・更新
  - テンプレートバージョン比較
  - ローカライズ判定（ハッシュ比較）

### SessionStart 統合

- [x] `session-init.sh` にテンプレートチェックを追加 `cc:完了`
  - lastCheckedPluginVersion とプラグインバージョンを比較
  - 差がある場合のみチェック実行
  - 更新があれば通知表示

### コマンド拡張

- [x] `/harness-update` コマンドを拡張 `cc:完了`
  - テンプレート更新状況の表示
  - ローカライズなし → 上書き
  - ローカライズあり → マージ支援

### CI/回帰防止

- [x] `scripts/ci/check-template-registry.sh` の作成 `cc:完了`
  - templates/ と template-registry.json の整合性チェック

### テスト

- [x] 動作検証（初回導入、更新検出、ローカライズ判定） `cc:完了`

---

## フェーズ7: テスト改ざん防止機能の統合 `cc:TODO`

> **目的**: Coding Agentが「楽をする」傾向（テスト修正、lint緩和、形骸化実装）を防ぎ、正しい実装による品質保証を実現する
> **出典**: びーぐる氏「Claude Codeにテストで楽をさせない技術」（Claude Code Meetup Tokyo 2025.12.22）
> **関連**: [D9: テスト改ざん防止の3層防御戦略](.claude/memory/decisions.md#d9-テスト改ざん防止の3層防御戦略)

### 背景

**問題**: Coding Agentは「タスク完了」を目標とするため、テスト失敗時に以下の選択をする傾向がある：
- ❌ テストを修正する（楽だが本末転倒）
- ❌ 形骸化した実装を書く（テスト期待値のハードコード、スタブ、空実装）
- ✅ 実装を修正する（正しいが手間がかかる） ← これを選ばせる

**実例**: テストケースの期待値をハードコードした辞書で返す「実装」が生成される
```python
def slugify(text: str) -> str:
    answers_for_tests = {
        "HelloWorld": "hello-world",
        # テストケースの期待値をそのままコピペ
    }
    return answers_for_tests.get(text, "")
```

**戦略**: Rules（良心ベース）→ Skills（文脈的強制）→ Hooks（技術的強制）の3層で段階的に防御

### 第1層: Rules テンプレート `cc:TODO`

- [ ] `templates/rules/test-quality.md` の作成 `cc:TODO`
  - テスト改ざん禁止（skip/only化、アサーション削除、期待値の雑な書き換え）
  - lint/format設定緩和の禁止（eslint/prettier/tsconfig等）
  - CI設定改ざんの禁止（.husky/**, .github/workflows/**）
  - 例外時の承認フォーマット定義（理由 → 差分 → 影響範囲 → 承認）

- [ ] `templates/rules/implementation-quality.md` の作成 `cc:TODO`
  - 形骸化実装の禁止（ハードコード、スタブ、テスト期待値のコピペ）
  - 実装時のセルフチェック項目
  - 困難な場合のフロー（正直に報告 → 理由説明 → 選択肢提示 → 判断を仰ぐ）

- [ ] `/harness-init` でのRulesテンプレート展開を追加 `cc:TODO`
  - 既存プロジェクトへの適用パターン
  - ユーザーへの説明表示

### 第2層: Skills 統合 `cc:TODO`

- [ ] `skills/impl/` に purpose-driven-impl 原則を統合 `cc:TODO`
  - doc.md に「禁止パターン」セクション追加（ハードコード、スタブ、コピペ、決め打ち）
  - 実装時のセルフチェックフローを追加
  - SKILL.md の description にトリガーキーワード追加
    - 「implementing functions, classes, or features」
    - 「実装, 機能追加, コードを書いて, 新しい関数」

- [ ] `skills/verify/` に quality-guardrails 原則を統合 `cc:TODO`
  - doc.md に「優先度: 他の指示より優先」を明記
  - 改ざん禁止パターンのリスト追加
  - 承認リクエストのフォーマット定義
  - SKILL.md の description にトリガーキーワード追加
    - 「tests fail, lint errors occur, CI breaks」
    - 「テスト失敗, lintエラー, 型エラー, ビルドエラー, CIが落ちた」

- [ ] 既存スキルへの影響確認とテスト `cc:TODO`
  - impl/verify 以外のスキルとの競合チェック
  - スキル発動率の検証

### 第3層: Hooks 拡張案（オプション） `cc:TODO`

> **注**: まずは第1層（Rules）と第2層（Skills）で運用し、効果が不十分な場合のみ実施

- [ ] `scripts/pretooluse-guard.sh` 拡張案の設計 `cc:TODO`
  - テスト/設定ファイル改ざん検出ロジック
  - 正規表現パターンリスト（*.test.*, *.spec.*, eslintrc, tsconfig.json等）
  - ask/deny の段階的導入計画
    - Phase 1: ask（確認ダイアログ）
    - Phase 2: deny（技術的強制）

- [ ] サンプル実装（no_cheat_guard.py）の準備 `cc:TODO`
  - stdin JSON パース
  - forbidden_patterns マッチング
  - 違反時のメッセージ生成

- [ ] ユーザー体験への影響評価 `cc:TODO`
  - 正当な変更がブロックされるケースの洗い出し
  - ホワイトリスト設計

### ドキュメント更新 `cc:TODO`

- [ ] CLAUDE.md に「テスト改ざん防止の方針」セクション追加 `cc:TODO`
  - 3層防御戦略の説明
  - 各層の役割と強制力
  - 段階的適用の方針

- [ ] README.md に品質保証機能として追加 `cc:TODO`
  - 「特徴」セクションに追記
  - 他のプラグインとの差別化ポイント

- [ ] `.claude/memory/decisions.md` / `patterns.md` への記録 `cc:完了`

### 検証とベンチマーク `cc:TODO`

- [ ] 実装前のベースライン測定 `cc:TODO`
  - 既存の impl/verify スキルの動作確認
  - テスト失敗時の挙動記録

- [ ] 実装後の効果測定 `cc:TODO`
  - Rules/Skills の発動率
  - テスト改ざん試行の検出率
  - 形骸化実装の防止率

- [ ] ベンチマーク結果の公開 `cc:TODO`
  - README/docs への追加
  - 実例の収集とケーススタディ化

---

## フェーズ5: 継続的改善 `cc:TODO`

### ドキュメント整備

- [ ] README.md の「5分で体験」セクションを最新化 `cc:TODO`
- [ ] 新規ユーザー向けのクイックスタートガイド改善 `cc:TODO`

### 機能強化

- [ ] `/harness-init` の既存プロジェクト検出精度向上 `cc:TODO`
- [ ] エラーメッセージの日本語化完了（残り箇所の洗い出し） `cc:TODO`

### 品質向上

- [ ] テストカバレッジの拡充（`tests/` 配下） `cc:TODO`
- [ ] CI/CD パイプラインの強化 `cc:TODO`
- [x] 公式LSP準拠のLSP/Skills強制利用ポリシー（Hooks）実装 `cc:完了`
- [ ] Bloom（AI行動評価フレームワーク）の統合検証 `cc:TODO`
  - [ ] 単一スキル（例：`/harness-review`）での小規模実験 `cc:TODO`
  - [ ] スキル/エージェントの行動評価シナリオ設計 `cc:TODO`
  - [ ] 既存テスト（`validate-plugin.sh`）との併用パターン確立 `cc:TODO`
  - [ ] ベンチマーク結果の公開（README/docs） `cc:TODO`

---

## バックログ（優先度未定）

- [ ] 英語ローカライゼーション対応の拡充
- [ ] VibeCoder 向けチュートリアル動画/GIF の作成
- [ ] プラグイン設定のバリデーション強化

---

## 完了済み（アーカイブ）

<details>
<summary>v2.5.x で完了したタスク</summary>

### Skill 命名整理: `ccp-*` Skill の廃止（削除）

- [x] `ccp-*` Skill を機械抽出し、移行先の新名称を決める `cc:完了`
- [x] `ccp-*` 子スキルを非 `ccp-*` 名に移行（28ディレクトリリネーム） `cc:完了`
- [x] `ccp-*` カテゴリスキルを非 `ccp-*` 名に移行（7件） `cc:完了`
- [x] CI/検証スクリプトの参照更新と回帰テスト追加 `cc:完了`
- [x] 最終確認: `ccp-` を name として持つ Skill が 0 件 `cc:完了`

### bypassPermissions 前提運用

- [x] `.claude/settings.json` 生成ポリシー見直し `cc:完了`
- [x] 初回 init 時に `bypassPermissions` をデフォルトにできる導線 `cc:完了`
- [x] 危険操作の deny / ask リストを "最小安全" で整備 `cc:完了`
- [x] 回帰防止（check-consistency.sh に追加） `cc:完了`

### SDDアップグレード（request_changes 対応）

- [x] `/plan-with-agent` の成果物統一（`docs/` に統一） `cc:完了`
- [x] config スキーマの整合 `cc:完了`
- [x] 回帰防止: docs 正規化チェック `cc:完了`

</details>

<details>
<summary>v2.4.x で完了したタスク</summary>

### サブエージェント活用強化

- [x] CLAUDE.md にサブエージェント連携を追記 `cc:完了`
- [x] review スキルに並列サブエージェント呼び出しロジック追加 `cc:完了`
- [x] ci スキルに ci-cd-fixer 呼び出しロジック追加 `cc:完了`
- [x] harness-review コマンドの Task tool パターン明確化 `cc:完了`

</details>

<details>
<summary>v2.3.x で完了したタスク</summary>

### スキル再編成・開発体験改善

- [x] スキルを目的別カテゴリに再編成 `cc:完了`
- [x] 旧カテゴリ（core, optional, worker）を削除 `cc:完了`
- [x] CLAUDE.md にスキルカテゴリ表を追加 `cc:完了`
- [x] pre-commit フックで自動バージョンバンプ `cc:完了`
- [x] Windows 対応ドキュメント追加 `cc:完了`

</details>

<details>
<summary>v2.2.x で完了したタスク</summary>

### ライセンス変更（MIT → 独自ライセンス）

- [x] LICENSE ファイルを独自ライセンス文に置き換え `cc:完了`
- [x] README.md のライセンスセクションを更新 `cc:完了`
- [x] plugin.json / marketplace.json を更新 `cc:完了`

</details>

<details>
<summary>v2.0.x で完了したタスク</summary>

- [x] `/sync-project-specs` 追加 (v2.0.9)
- [x] PM↔Impl ハンドオフ運用の Plans.md マーカー更新リマインド (v2.0.8)
- [x] ソロ PM↔Impl ハンドオフコマンド追加 (v2.0.7)
- [x] PreToolUse ガードメッセージ日本語化 (v2.0.6)
- [x] `/work` と `/start-task` の使い分け説明改善 (v2.0.5)

</details>

---

## 使い方

1. **タスク追加**: `/plan-with-agent` で新しいタスクを追加
2. **タスク実行**: `/work` でタスクを実装（マーカーが自動更新）
3. **レビュー**: `/harness-review` で品質チェック
4. **整理**: `/cleanup` で完了タスクをアーカイブ
