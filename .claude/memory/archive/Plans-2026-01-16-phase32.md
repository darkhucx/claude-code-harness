# Plans.md アーカイブ - フェーズ32 (2026-01-16)

> アーカイブ日: 2026-01-16
> 理由: フェーズ32完了、フェーズ33進行中

---

## 🟢 フェーズ32: task-worker 統合（Parallel Full-Cycle Automation） `cc:完了`

> **目的**: `/work --full` で「実装→セルフレビュー→改善→commit」のフルサイクルを並列自動化
> **優先度**: harness-ui より優先
> **設計思想**: サブエージェント内で完結させ、Task tool 制限を回避

### 要件サマリ

| 項目 | 決定 |
|------|------|
| セルフレビュー深度 | 標準（品質 + セキュリティ + パフォーマンス + 互換性） |
| クロスレビュー（Phase 2） | 常に実行（Codex 8並列） |
| Commit 戦略 | デフォ: タスクごと、オプション: フェーズごと / 全完了後 |
| 並列数 | デフォ: 1、上限: 5（設定可能） |
| 改善ループ上限 | 3回（超過でエスカレーション） |
| エスカレーション | 親に集約 → 一括でユーザーに確認 |
| Deploy | オプション（--deploy フラグ + 安全ゲート） |
| コマンド設計 | 既存 /work を拡張（--full オプション追加） |
| 知識範囲 | ハイブリッド（コアルール内包 + 詳細は参照） |
| ワークスペース分離 | デフォ: ファイルロック、オプション: worktree分離 |

### Codex レビュー結果（反映済み）

| 重篤度 | 指摘 | 対策 |
|--------|------|------|
| Critical | 共有ワークスペース競合 | ファイルロック or worktree分離 |
| Critical | 並列ビルドのキャッシュ破壊 | 直列実行 or worktree分離+pnpm |
| Major | エスカレーション並列多発 | 親に集約し一括確認 |
| Major | commit_ready 条件曖昧 | 基準を明文化（下記） |
| Major | Plans.md 同時編集衝突 | 親のみが更新可能 |
| Medium | レビュー観点不足 | 互換性/回帰を追加 |
| Medium | 並列数上限なしのリスク | デフォ5上限に変更 |

### commit_ready 基準（明文化）

タスクが `commit_ready` を返すには以下を**全て**満たすこと：
1. セルフレビュー全観点で Critical/Major 指摘なし
2. `npm run build` または該当ビルドコマンドが成功
3. `npm test` または該当テストが成功（該当ファイルのみ可）
4. 既存テストの回帰なし

---

### 32.1 task-worker エージェント作成 `cc:完了` `[feature:tdd]`

**ファイル**: `agents/task-worker.md`

**テストケース（セルフレビュー判定）**:

| 入力（実装結果） | 期待出力 | 備考 |
|-----------------|---------|------|
| 品質OK + セキュリティOK + パフォーマンスOK | `commit_ready` | 全観点クリア |
| 品質NG（命名不適切） | 自己修正 → 再レビュー | ループ1回目 |
| 3回修正しても NG | `needs_escalation` | ユーザー判断 |
| ビルド失敗 | 自己修正 → 再ビルド | verify 知識適用 |

**実装タスク**:
- [x] `agents/task-worker.md` 雛形作成
- [x] 内部フロー定義（実装→レビュー→修正→検証）
- [x] 品質ガードレール内包（禁止パターン）
- [x] セルフレビュー 4観点チェックリスト（品質/セキュリティ/パフォーマンス/互換性）
- [x] エスカレーション条件定義
- [x] 入出力スキーマ定義

---

### 32.2 /work コマンド拡張 `cc:完了`

**ファイル**: `commands/core/work.md`

**追加オプション**:

| オプション | 説明 | デフォルト |
|-----------|------|-----------|
| `--full` | フルサイクル実行 | false |
| `--parallel N` | 並列数指定 | 1 |
| `--isolation` | lock / worktree | lock |
| `--commit-strategy` | task / phase / all | task |
| `--deploy` | commit 後に deploy | false |
| `--max-iterations` | 改善ループ上限 | 3 |
| `--skip-cross-review` | Phase 2 スキップ | false |

**--isolation=worktree の動作**:
- 各タスクに `git worktree add` でブランチ作成
- pnpm 使用時はシンボリックリンクで容量節約（+54MB/worktree）
- 並列ビルド/テストが可能（完全分離）
- 完了後に main へマージ → worktree 削除

**実装タスク**:
- [x] work.md にオプション定義追加
- [x] --full モードのフロー記述
- [x] Phase 1/2/3 のオーケストレーション手順
- [x] エラーハンドリング（一部失敗時）
- [x] 統合レポート形式更新

---

### 32.3 Phase 1: 並列実装 + セルフレビュー `cc:完了` `[feature:tdd]`

**テストケース（オーケストレーション）**:

| 入力（Plans.md） | 期待動作 | 備考 |
|-----------------|---------|------|
| 独立3タスク | 3 task-worker 並列起動 | 依存なし |
| A→B 依存あり | A 完了後に B 起動 | 依存グラフ尊重 |
| 同一ファイル編集 | 直列実行 | コンフリクト回避 |

**実装タスク**:
- [x] 依存グラフ構築ロジック
- [x] task-worker 並列起動処理
- [x] 結果収集（TaskOutput）
- [x] 進捗表示

---

### 32.4 Phase 2: クロスレビュー（Codex 8並列） `cc:完了`

**実装タスク**:
- [x] Phase 1 完了判定
- [x] Codex 8並列レビュー呼び出し
- [x] 追加指摘の抽出
- [x] 問題あり → Phase 1 へフィードバック

---

### 32.5 Phase 3: 統合 Commit `cc:完了`

**Commit 戦略の実装**:

```
--commit-strategy=task:
  各タスク完了時に個別 commit

--commit-strategy=phase:
  フェーズ完了時にまとめて commit

--commit-strategy=all:
  全タスク + クロスレビュー完了後に 1 commit
```

**実装タスク**:
- [x] コンフリクト検出・解消
- [x] 最終ビルド検証
- [x] Conventional Commit 生成
- [x] 戦略別の commit 実行

---

### 32.6 Deploy 統合（オプション） `cc:完了`

**実装タスク**:
- [x] --deploy フラグ処理
- [x] deploy スキル呼び出し
- [x] 結果レポート

---

### 32.7 ドキュメント & テスト `cc:完了`

**実装タスク**:
- [x] CHANGELOG.md 更新
- [x] docs/PARALLEL_FULL_CYCLE.md 作成
- [x] 手動テストチェックリスト（ドキュメントに含む）

---

## 🔵 フェーズ31: harness-ui MVP `cc:blocked`

> **ブロック理由**: フェーズ32 を優先するため一時保留
> **詳細仕様**: [phase31-harness-ui-spec.md](.claude/memory/phase31-harness-ui-spec.md)

サブフェーズ 31.0〜31.6（詳細は仕様書参照）
