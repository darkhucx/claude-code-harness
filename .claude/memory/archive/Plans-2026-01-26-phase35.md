# Archive: Phase 35 - opencode-setup フル互換化

**アーカイブ日**: 2026-01-26
**ステータス**: 完了

---

## Phase 35: opencode-setup フル互換化 `cc:完了`

> **目的**: `/opencode-setup` で harness のスキル・コマンド・ルールを opencode.ai でもフルに使えるようにする
>
> **背景**: opencode.ai は `.claude/skills/` と `CLAUDE.md` をフォールバックとしてサポート（Codex レビュー済み）

### 35.1 build-opencode.js 更新 `cc:完了`

- [x] `opencode/AGENTS.md` を `CLAUDE.md` 全文コピーで生成するように変更
- [x] `opencode/skills/` にスキルをコピーする機能を追加
- [x] 検証: `node scripts/build-opencode.js` 実行後、AGENTS.md が CLAUDE.md と同一か確認

**対象ファイル**: `scripts/build-opencode.js`

### 35.2 /opencode-setup 手順更新 `cc:完了`

- [x] Deliverables に `.claude/skills/` を追加
- [x] Step: `.claude/skills/` ディレクトリ作成（既存ならバックアップ）
- [x] Step: `skills/` → `.claude/skills/` へコピー（symlink はオプション）
- [x] Step: `AGENTS.md` は `$PLUGIN_DIR/opencode/AGENTS.md` をコピー
- [x] Windows 対応のためコピーをデフォルト、symlink はオプション扱い

**対象ファイル**: `commands/optional/opencode-setup.md`

### 35.3 opencode コマンド再生成 `cc:完了`

- [x] `node scripts/build-opencode.js` 実行
- [x] 生成物の確認（32 コマンド、26 スキル）

**対象ファイル**: `opencode/` ディレクトリ（生成物）

### 35.4 setup-opencode.sh 同期 `cc:完了`

- [x] `/opencode-setup` と同等の処理を追加
- [x] `.claude/skills/` コピー処理追加
- [x] AGENTS.md = CLAUDE.md 全文
- [x] 完了メッセージに利用可能なスキル一覧を追加

**対象ファイル**: `scripts/setup-opencode.sh`

### 35.5 検証 `cc:完了`

- [x] ビルドスクリプト実行確認
- [x] 32 コマンド、26 スキルが生成されることを確認
- [x] AGENTS.md が CLAUDE.md の内容を含むことを確認
- [x] docs スキルに references/ と SKILL.md が含まれることを確認

---

### 技術メモ（Codex レビュー結果）

| 指摘 | 対応 |
|------|------|
| 変換責務の二重化リスク | `build-opencode.js` を SSOT に、`/opencode-setup` はコピーのみ |
| symlink は setup-opencode.sh で破綻 | コピーをデフォルトに変更 |
| allowed-tools の互換性 | opencode のツール名と不一致の可能性（要確認） |
